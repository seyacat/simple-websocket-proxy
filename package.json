{
  "name": "websocket-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy with UUID validation using ws",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --exec node server.js",
    "dev:env": "nodemon -r dotenv/config server.js"
  },
  "dependencies": {
    "ws": "^8.14.2",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": ["websocket", "proxy", "ws"],
  "author": "",
  "license": "MIT"
}
